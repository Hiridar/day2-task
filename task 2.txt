  1.   DOCUMENT:
            * If represents any HTML document or web page  that is loaded in the browser.
            * All the tags, elements with attributes in HTML are part of the document.
            * The document is part of DOM (Document object model) & BOM (Browser object model).
            * Properties of document objects such as title, body, cookies, etc can also be accessed by a window 
                like this window,document.title.
          Example:
                  Document.title: will return the title of the document.

  2.   WINDOW:
            * It represent a browser window or frame the displays the contents of the webpage.
            * Global objects, functions, and variables of javascript are members of the window object.
            * The window is part of BOM, not DOM.
            * Properties of the window object cannot be accessed by the document object.
          Example:
                  Window.innerHeight:will return the height of the content area of the browser.